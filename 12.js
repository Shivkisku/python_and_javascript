// Given angles
const angle1 = 15; // replace with the actual value of the first angle
const angle2 = 85; // replace with the actual value of the second angle

// Calculate the measure of the third angle
const thirdAngle = 180 - angle1 - angle2;

// Print the result
console.log("The measure of the third angle is:", thirdAngle);

// Given values
const y = 2; // replace with the actual value of y
const z = 10; // replace with the actual value of z

// Solve for x
const x = z / y + 1;

// Print the result
console.log("The solution for x is:", x);
